import wollok.game.*
import Personaje.*
import Nivel.*

import laGrietaDelInvocador.*

program juego {
    organizador.cargarListaDeNiveles([aguasEstancadas])
    organizador.actualizar()
    game.addVisual(informadorDeErrores)
	game.errorReporter(informadorDeErrores)
    game.title("Aventura De Portales")
    game.showAttributes(atrox)
    game.width(14) 
	game.height(7)
	game.start()
}